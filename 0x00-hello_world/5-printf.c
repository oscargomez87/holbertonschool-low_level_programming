#include <stdio.h>

/*
 *Prints exactly with proper grammar, but the outcome is a piece of art,
 *followed by a new line
 */

/**
 * main - Prints exactly "with proper grammar, but the outcome is
 *a piece of art," followed by a new line
 * Return: 0 when everthing is ok
 */
int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);

	return (0);
}
